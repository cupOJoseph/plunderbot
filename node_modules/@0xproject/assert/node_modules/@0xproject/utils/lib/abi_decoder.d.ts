import { LogWithDecodedArgs, RawLog } from '@0xproject/types';
import * as Web3 from 'web3';
export declare class AbiDecoder {
    private _savedABIs;
    private _methodIds;
    private static _padZeros(address);
    constructor(abiArrays: Web3.AbiDefinition[][]);
    tryToDecodeLogOrNoop<ArgsType>(log: Web3.LogEntry): LogWithDecodedArgs<ArgsType> | RawLog;
    private _addABI(abiArray);
}
