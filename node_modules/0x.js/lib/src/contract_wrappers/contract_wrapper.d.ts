import { Web3Wrapper } from '@0xproject/web3-wrapper';
import * as Web3 from 'web3';
import { Artifact, BlockRange, ContractEventArgs, ContractEvents, EventCallback, IndexedFilterValues, LogWithDecodedArgs, RawLog } from '../types';
import { AbiDecoder } from '../utils/abi_decoder';
export declare class ContractWrapper {
    protected _web3Wrapper: Web3Wrapper;
    private _networkId;
    private _abiDecoder?;
    private _blockAndLogStreamerIfExists;
    private _blockAndLogStreamInterval;
    private _filters;
    private _filterCallbacks;
    private _onLogAddedSubscriptionToken;
    private _onLogRemovedSubscriptionToken;
    constructor(web3Wrapper: Web3Wrapper, networkId: number, abiDecoder?: AbiDecoder);
    protected unsubscribeAll(): void;
    protected _unsubscribe(filterToken: string, err?: Error): void;
    protected _subscribe<ArgsType extends ContractEventArgs>(address: string, eventName: ContractEvents, indexFilterValues: IndexedFilterValues, abi: Web3.ContractAbi, callback: EventCallback<ArgsType>): string;
    protected _getLogsAsync<ArgsType extends ContractEventArgs>(address: string, eventName: ContractEvents, blockRange: BlockRange, indexFilterValues: IndexedFilterValues, abi: Web3.ContractAbi): Promise<Array<LogWithDecodedArgs<ArgsType>>>;
    protected _tryToDecodeLogOrNoop<ArgsType extends ContractEventArgs>(log: Web3.LogEntry): LogWithDecodedArgs<ArgsType> | RawLog;
    protected _instantiateContractIfExistsAsync(artifact: Artifact, addressIfExists?: string): Promise<Web3.ContractInstance>;
    protected _getContractAddress(artifact: Artifact, addressIfExists?: string): string;
    private _onLogStateChanged<ArgsType>(isRemoved, log);
    private _startBlockAndLogStream();
    private _setNetworkId(networkId);
    private _stopBlockAndLogStream();
    private _reconcileBlockAsync();
}
